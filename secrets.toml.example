# Copy to .streamlit/secrets.toml for local runs (never commit the real file),
# or paste into Streamlit Community Cloud's app Settings -> Secrets.

ANTHROPIC_API_KEY = "sk-ant-..."
# OPENAI_API_KEY = "sk-..."
